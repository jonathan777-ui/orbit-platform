# Python
__pycache__/
*.pyc

# OS
.DS_Store

# The built site/_site is committed so Cloudflare Workers can deploy it without
# a build step. Rebuild after content changes with `python site/build.py`.
site/*.zip
